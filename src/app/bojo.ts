declare module namespace {

        export interface Restrico {
        restricao: string;
    }

    export interface Bin {
        ano_modelo: string;
        chassi: string;
        marca: string;
        placa: string;
    }

    export interface DadosGravame {
        ano_modelo: string;
        placa: string;
    }

    export interface Gravames {
        chassi: string;
     }

}